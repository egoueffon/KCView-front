import { EncaissementPipe } from './encaissement.pipe';

describe('EncaissementPipe', () => {
  it('create an instance', () => {
    const pipe = new EncaissementPipe();
    expect(pipe).toBeTruthy();
  });
});
